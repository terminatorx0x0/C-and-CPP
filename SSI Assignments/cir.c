#include <stdio.h>
#include <conio.h>
float main()
{
float a,re;
printf("enter the radius of the circle in metre ");
scanf("%f",&a);
re=((22)*(a*a))/7;
printf("the area of circle is %f meter sq ",re);
return 0;
}
