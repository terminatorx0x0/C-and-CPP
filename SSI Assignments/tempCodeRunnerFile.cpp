#include<iostream>
#include<string.h>
using namespace std;
int main()
{
    char s[20],ch;
    int i,j;
    cout<<"enter the string:";
    cin>>s;
    j=strlen(s);
    for(i=0;i<x;i++)
    {
        if(s[i]=='a' || s[x]=='b')
        {
            cout<<"";
        }
        else{
            cout<<"wrong input!!";
            cout<<endl<<"enter again:";
            cin>>s;
            j=strlen(s);
            i=0;
        }
    }
}